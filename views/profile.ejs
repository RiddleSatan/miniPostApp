<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Blogs</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10">
      <div class="w-full flex justify-between">
        <h1 class="text-2xl text-white mb-4">This is your Profile</h1>
        <form action="/logout" method="post">
          <input
            class="p-2 bg-red-500 text-white rounded-md"
            type="submit"
            value="Logout"
          />
        </form>
      </div>
      <h1 class="text-orange-500 mb-2">
        welcome <%=data.username%> how u doin
      </h1>
      <form
        class="text-white"
        action="/post/<%=data._id%>"
        method="post"
        class=""
      >
        <textarea
          name="post"
          id=""
          class="block w-1/4 h-20 rounded-md bg-zinc-800 outline-none border-2 border-zinc-600 p-2"
          placeholder="what's on your mind?"
        ></textarea>
        <input
          class="px-4 py-1 mt-2 rounded-md bg-blue-500"
          type="submit"
          value="Post"
          name=""
          id=""
        />
      </form>
      <div class="mt-20">
        <h1 class="ml-2 text-zinc-500 mb-5">your posts.</h1>
        <%data.posts.forEach((post)=>{%>

        <div
          class="w-1/3 bg-zinc-800 mb-4 py-2 px-4 rounded-md border-[1px] border-zinc-600"
        >
          <h2 class="text-blue-500 mb-3"><%=data.username%></h2>
          <p class="text-white tracking-tighter"><%=post.postData%></p>
          <div class="mt-2 flex gap-4">
            <a class="text-blue-500" href="">Like</a>
            <a class="text-zinc-500" href="">edit</a>
          </div>
        </div>
        <%})%>
      </div>
    </div>
    <script src="../public/javascripts/index.js"></script>
    
  </body>
</html>
